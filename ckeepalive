#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
INTERVAL="${1:-300}"

echo "Running creconnect --all every ${INTERVAL}s (Ctrl-C to stop)"

while true; do
  "${SCRIPT_DIR}/creconnect" --all
  sleep "$INTERVAL"
done
